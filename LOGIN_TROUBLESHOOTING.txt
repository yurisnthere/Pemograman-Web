╔═══════════════════════════════════════════════════════════════╗
║              🔧 TROUBLESHOOTING LOGIN ISSUES                  ║
╚═══════════════════════════════════════════════════════════════╝

🎯 MASALAH: Tidak bisa login dengan user lama (admin/user dari seed)

═══════════════════════════════════════════════════════════════

✅ SOLUSI YANG SUDAH DITERAPKAN:

1. Enhanced logging di app.js
   - Menampilkan detail request/response di Console
   - Memudahkan debugging masalah login

2. Browser cache handling
   - Cache busting dengan ?v={{ time() }}
   - Force reload JavaScript terbaru

3. User Model cleanup
   - Memastikan tidak ada interferensi dengan password

═══════════════════════════════════════════════════════════════

📋 CARA TROUBLESHOOT:

LANGKAH 1: HARD REFRESH BROWSER
---------------------------------
Windows: Ctrl + Shift + R
Mac: Cmd + Shift + R

Ini akan clear cache dan load JavaScript terbaru.


LANGKAH 2: BUKA CONSOLE (F12)
---------------------------------
1. Tekan F12 untuk buka Developer Tools
2. Pilih tab "Console"
3. Coba login
4. Lihat log yang muncul:

   ✅ SUKSES (akan tampil):
   === LOGIN ATTEMPT ===
   Username: admin
   Password length: 8
   Sending request to /api/login...
   Response status: 200
   Response ok: true
   Response data: {message: "Login berhasil", user: {...}}
   Login successful! User: {...}

   ❌ GAGAL (akan tampil):
   === LOGIN ATTEMPT ===
   Username: admin
   Password length: 8
   Sending request to /api/login...
   Response status: 401
   Response ok: false
   Response data: {error: "Username atau password salah"}
   Login failed: Username atau password salah


LANGKAH 3: TEST API LANGSUNG
---------------------------------
Buka: http://127.0.0.1:8000/test-login.html

- Username dan password sudah terisi otomatis (admin/admin123)
- Klik "Test Login"
- Jika berhasil → masalah ada di browser cache
- Jika gagal → masalah ada di backend


LANGKAH 4: VERIFIKASI DATABASE
---------------------------------
Di terminal PowerShell, jalankan:

php -r "require 'vendor/autoload.php'; \$app = require_once 'bootstrap/app.php'; \$app->make('Illuminate\Contracts\Console\Kernel')->bootstrap(); \$user = App\Models\User::where('username', 'admin')->first(); \$check = Illuminate\Support\Facades\Hash::check('admin123', \$user->password); echo 'Password check: ' . (\$check ? 'OK' : 'FAIL');"

Harus output: Password check: OK

═══════════════════════════════════════════════════════════════

🔑 KREDENSIAL LOGIN:

ADMIN:
Username: admin
Password: admin123

USER:
Username: user
Password: user123

═══════════════════════════════════════════════════════════════

⚠️ COMMON ISSUES:

1. "Username atau password salah" tapi yakin benar
   → Hard refresh browser (Ctrl+Shift+R)
   → Clear browser cache
   → Test di incognito mode

2. Form tidak merespon
   → Cek Console untuk error JavaScript
   → Pastikan server running (php artisan serve)

3. Error 404 saat submit
   → Pastikan URL adalah /warteg bukan root
   → Cek apakah server Laravel running

4. Password salah terus
   → Coba reset database: php artisan migrate:fresh --seed
   → Password akan kembali ke default

═══════════════════════════════════════════════════════════════

🧪 TESTING RESULTS:

✅ Backend API: WORKING
   - curl test berhasil
   - Hash::check berhasil verifikasi password
   - Response JSON benar

✅ Database: WORKING
   - Password ter-hash dengan benar ($2y$12$...)
   - User admin & user ada di database
   - Timestamps correct

✅ Frontend: FIXED
   - Enhanced logging
   - Better error handling
   - Cache busting enabled

═══════════════════════════════════════════════════════════════

💡 NEXT STEPS:

1. Hard refresh browser (Ctrl+Shift+R)
2. Buka Console (F12)
3. Login dengan admin/admin123
4. Cek log di Console
5. Screenshot log jika masih error

═══════════════════════════════════════════════════════════════

📧 DEBUGGING INFO TO SHARE:

Jika masih bermasalah, share info berikut:
- Browser yang digunakan
- Screenshot Console log (F12 → Console)
- Screenshot Network tab (F12 → Network → filter: login)
- Error message yang muncul

═══════════════════════════════════════════════════════════════
